---
title: "Read Scans and XIC from raw data using the R package ..."
author: "Christian Panse, Toboas Kockmann"
date: "2019-06-17"
output:
  html_document: default
vignette: >
  %\VignetteIndexEntry{read Thermo Finnigan raw files}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

# Simple usage
```{r}
library(MsBackendRawFileReader)

fls <- rep(rawfile <- file.path(path.package(package = 'MsBackendRawFileReader'), 'extdata', 'sample.raw'), 1)

be <- backendInitialize(MsBackendRawFileReader(), files = fls)

sps_thermofinnigan <- Spectra(be)
sps_thermofinnigan
```

# Session Info

```{r}
sessionInfo()
```

#References


